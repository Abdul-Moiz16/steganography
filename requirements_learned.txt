# Additional dependencies for the SRNet / DCTR learned baselines.
# The main pipeline (run.py prototype_full ...) does NOT need these;
# they are only required when training or running inference for the
# learned-detector branch.
#
#   pip install -r requirements.txt -r requirements_learned.txt

# SRNet (PyTorch model + training loop + inference)
torch>=2.1
torchvision>=0.16
tqdm>=4.65

# DCTR (feature extractor + linear classifier)
scikit-learn>=1.3
joblib>=1.3

# Optional: TensorBoard for monitoring training (otherwise plain stdout)
tensorboard>=2.14
